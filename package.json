{
  "name": "credit-risk-scoring-engine",
  "version": "2.0.0",
  "private": true,
  "description": "Deployable MERN credit risk scoring app with a Python ML service.",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "start": "npm start --prefix server",
    "train": "python -m src.train",
    "train:india": "python -m src.train_india_loan",
    "ml": "uvicorn src.api:app --reload --host 127.0.0.1 --port 8000"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
