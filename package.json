{
  "name": "credit-risk-scoring-engine",
  "version": "2.0.0",
  "private": true,
  "description": "Deploy-ready FastAPI and React credit risk scoring engine.",
  "scripts": {
    "install:all": "npm install --prefix frontend",
    "dev": "concurrently \"python -m uvicorn main:app --reload --reload-dir backend --port 8000 --app-dir backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "backend": "python -m uvicorn main:app --reload --reload-dir backend --port 8000 --app-dir backend",
    "frontend": "npm run dev --prefix frontend",
    "train": "python backend/train_model.py",
    "legacy:dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "legacy:ml": "uvicorn src.api:app --reload --host 127.0.0.1 --port 8000"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
